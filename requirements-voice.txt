# Optional dependencies for real voice control (STT + local LLM intent
# resolution). NOT required to run the base app or its test suite - see
# ARCHITECTURE.md "Voice control" and jarvis.voice_capture / jarvis.llm_intent
# for why these are lazy-imported instead of listed in requirements.txt.
#
#   pip install -r requirements.txt -r requirements-voice.txt
#
faster-whisper>=1.0.0
llama-cpp-python>=0.3.0
sounddevice>=0.4.6
